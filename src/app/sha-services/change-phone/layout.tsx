import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'SHA Phone Number Change Online | Update Registered Number',
  description: 'Easily update your SHA/NHIF registered phone number online. Fast processing by certified agents. Required: National ID and Police Abstract.',
  keywords: ['SHA phone change', 'NHIF phone number change', 'update SHA number online', 'Social Health Authority phone update'],
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
