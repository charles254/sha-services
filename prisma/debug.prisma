generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:postgres@localhost:51216/postgres?sslmode=disable"
}

model Test {
  id Int @id @default(autoincrement())
}
